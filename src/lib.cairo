mod contracts {
    mod reflect_draft;
    mod reflect_V0;
    mod ERC20wrapper_v0;
}
mod interfaces {
    mod rinterface;
    mod winterface;
}
#[cfg(test)]
mod tests {
    mod reflect;
    mod utils;
}
