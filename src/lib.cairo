mod contracts {
    mod reflect_draft;
    mod reflect;
    mod ERC20wrapper;
    mod reflect_factory;
}
mod interfaces {
    mod rinterface;
    mod winterface;
    mod rinterfacev2;
}
#[cfg(test)]
mod tests {
    mod reflect;
    mod utils;
}
